<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="de.jbdevelop.punchaclock.ui.main.MainActivity">


    <android.support.v7.widget.Toolbar
        android:id="@+id/main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        />

    <ListView
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:id="@+id/area_list_view"
        android:layout_marginTop="?android:attr/actionBarSize">
    </ListView>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:onClick="addAreas"
        android:layout_margin="@dimen/fab_margin"
        android:src="@drawable/ic_add_24dp"/>

    <TextView
        android:text="@string/activity_main_service_disabledHint"
        android:layout_width="match_parent"
        android:id="@+id/service_disabled_hint"
        android:background="#f49842"
        android:layout_gravity="bottom"
        android:textSize="18sp"
        android:textAlignment="center"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"/>

</android.support.design.widget.CoordinatorLayout>
